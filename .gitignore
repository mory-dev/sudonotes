node_modules/
dist/
target/
# Isolated build targets used for local verification; Cargo's default target/
# rule does not cover these alternate names.
app/src-tauri/target-search-test*/
.DS_Store
*.log
.env
IDEAS.md
!site/src/content/docs/ideas.md
core/Cargo.lock
/.vscode
# Apt repository metadata under site/public/apt is rebuilt in CI from the
# release's .deb; pool/ accumulates and dists/ is regenerated each run.
site/public/apt/pool/
site/public/apt/dists/
